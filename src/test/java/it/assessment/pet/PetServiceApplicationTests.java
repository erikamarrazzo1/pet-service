package it.assessment.pet;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class PetServiceApplicationTests {

    @Test
    void contextLoads() {
        // This test verifies that the Spring ApplicationContext loads successfully.
        // If the context fails to start due to misconfiguration
        // (missing beans, invalid properties, or wiring issues), this test will fail.
    }

}
